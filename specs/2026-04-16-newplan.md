• 落地任务清单（先整合、后迁 UI）

- [x] 建立统一领域模型与结果协议（P0，0.5天）

  - 新建 src/handcontrol/core/types.py 的扩展类型：FirmwareType、SerialCommandResult、FlashJobResult。
  - 统一 ok/code/message/payload 返回约定，覆盖手控与音乐流程。
  - 验收：服务层接口签名固定，后续 UI 不再关心实现差异。

- [x] 抽离音乐版串口能力到服务层（P0，1-1.5天）

  - 新建 src/handcontrol/core/services/serial_service.py：串口扫描、连接、断开、读循环。
  - 新建 src/handcontrol/core/services/at_command_service.py：AT 发送、批量发送、AT+BD 波特率切换验证。
  - 从 specs/ui-reactor/music_flash_tool_1.py 删除业务逻辑，只保留 UI 事件调用。
  - 验收：同样逻辑可被不同 UI 复用。
  - 进度：已完成，`music_flash_tool_1.py` 串口/AT 逻辑改为调用 service（UI 仅保留事件与展示）。

- [x] 抽离音乐版 U 盘流程为复用服务（P0，0.5天）

  - 新建 src/handcontrol/core/services/music_flash_service.py：格式化→复制目录→弹出。
  - 复用 src/handcontrol/core/usb_ops.py 现有能力，补齐“复制目录”操作。
  - 验收：音乐版刷机不再直接 subprocess/shutil 写在 UI 中。

- [x] 配置化音乐版参数（P1，0.5天）

  - 扩展 config.toml：新增 [music]、[serial]。
  - 扩展 src/handcontrol/core/settings.py 读取默认音乐目录、AT 预设、默认波特率。
  - 验收：不改代码即可调整 AT 预设和默认路径。

- [x] 在现有主程序加“固件类型切换”（P1，1天）

  - 修改 src/handcontrol/app.py：增加“手控/音乐”模式切换入口。
  - 手控流程保持原样，新增音乐面板最小可用版（调用新服务层）。
  - 验收：一个程序内可完整跑两条流程。

- [x] 测试补齐（P1，1天）

  - [x] 新增 tests/test_serial_service.py。
  - [x] 新增 tests/test_at_command_service.py。
  - [x] 新增 tests/test_music_flash_service.py。
  - [x] 扩展 tests/test_app_service_smoke.py 覆盖模式切换。
  - 验收：.\scripts\test.ps1 全绿，核心回归稳定。

- [ ] 再做 CTk 迁移（P2，2-4天）

  - 以 specs/ui-reactor/new_uigemini.py 为视觉参考，拆成 handcontrol_panel 和 music_panel。
  - core/services 不再改动，只换 UI 壳。
  - 验收：功能一致，UI 升级不影响业务。

  执行顺序建议（严格按此）

- [x] 服务层抽离
- [x] 配置化
- [x] 单程序双模式
- [x] 测试补齐
- [ ] CTk 迁移
