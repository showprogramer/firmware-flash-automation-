---
### 问题 1：左侧栏 UI 构建仍集中在 FirmwareListPanel

**类型**： 重构

**模块**： `src/fwasset/ui/firmware_list_panel.py`, `src/fwasset/ui/panels/sidebar_panel.py`

**问题描述**
- `FirmwareListPanel._build_sidebar()` 同时负责搜索框、类型筛选、排序菜单、扫描按钮和资源树装配。
- 左侧栏控件变化会继续扩大 `FirmwareListPanel`，不利于 Issue 1 的装配器化目标。

**处理方案**
- 新增 `SidebarPanel`，集中构建左侧栏控件和 `AssetTreeView`。
- `FirmwareListPanel._build_sidebar()` 改为创建 `SidebarPanel` 并保留 `scan_btn`、`type_quick_menu`、`sort_menu`、`asset_tree` 兼容属性。
- 新增 `tests/test_sidebar_panel.py`，验证扫描按钮和资源树回调契约。

**状态**： ✅ 已修复

**验证记录**
- 验证日期：2026-05-18
- 验证人：Agent / 人工
- 验证结果：通过自动化测试与人工验证
- 对应版本：未发布

**相关 Commit**： e680def
---
